using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enums {
    /*
        OrbType Enum. Enum for current OrbType (No. of ORBS).
    */
    public enum OrbType
    {
        NONE,
        SINGLE,
        DOUBLE,
        TRIPLE
    }
}
