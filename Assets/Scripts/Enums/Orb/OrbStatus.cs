using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enums {
    /*
        OrbStatus Enum. Enum for current Orb Status (STABLE - LOW VIBRATION, UNSTABLE - HIGH VIBRATION)
    */
    public enum OrbStatus
    {
        NONE,
        STABLE,
        UNSTABLE
    }
}
