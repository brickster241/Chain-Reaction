using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enums {
    /*
        SoundType Enum. Enum for different sounds in the Game.
    */
    public enum SoundType 
    {
        UPDATE_TURN,
        BUTTON_CLICK,
        POP_CLICK,
        GAME_COMPLETE
    }
}
